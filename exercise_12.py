# Exercise 12
# Create a function that creates a list with only the first and last entries of a longer list.

import random


def first_last():
    return print(a[0], a[-1])


a = random.sample(range(1000), 7)
a = sorted(a)
print(a)
b = [first_last()]
